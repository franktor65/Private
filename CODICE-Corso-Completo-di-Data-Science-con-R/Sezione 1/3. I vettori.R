# usiamo la funzione c()

vettore.numerico <- c(1,4,6,7,9,12,15,17,21)

vettore.di.caratteri <- c("Londra", "Parigi", "Roma", "Madrid", "Atene")

vettore.logico <- c(TRUE, FALSE, TRUE, FALSE, FALSE)

vettore.logico.2 <- c(T,F,T,T,F,F)

assign("nome.vettore", c(2, 4, 7, 12, 15, 19))

nome.vettore.2 <- seq(from=1, to=20, by=2) 
nome.vettore.2

seq(1,20)

nome.vettore.3 <- seq(from=30, to=40, length=2) 
nome.vettore.3

